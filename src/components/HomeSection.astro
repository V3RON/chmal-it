---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
  containerClass?: string;
  id?: string;
}

const { class: className, containerClass, id } = Astro.props;
---

<section id={id} class={cn("py-12 md:py-20 lg:py-24", className)}>
  <div class={cn("container", containerClass)}>
    <slot />
  </div>
</section>
